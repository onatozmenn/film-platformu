# Playback Fixture

`guest-feature.mp4` is an original deterministic test asset generated for this repository on 2026-07-19 with FFmpeg's `testsrc2` video source and `sine` audio source. It contains no third-party film footage, music, brand, or personal data.

`guest-feature-tr.vtt` is original Turkish test copy created for the same fixture. It contains no dialogue or subtitle text from a film.

Generation settings: 640x360, 24 fps, six seconds, H.264/AAC, CRF 32. The asset is for local development and automated browser tests only; production playback uses operator-licensed media through the configured Mux account.
